
<script type="text/javascript">
    $(document).ready(function () {
        $('#jqGrid').jqGrid({
            url: '@Url.Action("GetList","BudgetHead")',
            datatype: 'json',
            mtype: 'POST',
            colNames: ['ID', 'Budget Head', 'Budget Sub-Head'],
            colModel: [
                            { name: 'ID', index: 'ID', align: 'left', search: false, hidden: true, edittype: 'text' },
                            //{ name: 'ParentId', index: 'ParentId', align: 'left', search: false, hidden: true, edittype: 'text' },
                            { name: 'BudgetHeadName', index: 'BudgetHeadName', align: 'left', search: true, edittype: 'text' },
                            { name: 'BudgetSubHeadName', index: 'BudgetSubHeadName', align: 'left', search: true, edittype: 'text' },
                            //{ name: 'CreatedBy', index: 'CreatedBy', align: 'center', search: false, edittype: 'text', width:"100px" },
                            //{ name: 'CreatedDate', index: 'CreatedDate', align: 'center', search: false, edittype: 'text', width:"100px"},
                            //{ name: 'IsActive', index: 'IsActive', align: 'center', search: false, edittype: 'text', width:"40px" }
                            //{ name: 'Delete', index: 'Delete', align: 'right', search: false, hidden: IsDeleteAssigned, sortable: false, width: '50px', edittype: 'image', formatter: deleteformateadorLink }
            ],
            autowidth: true,
            caption: "Budget Head & Sub-Head",
            rowList: [20, 30, 40, 50, 100],
            pager: $('#jqGridPager'),
            rowNum: 20,
            sortname: 'ID',
            sortorder: 'asc',
            viewrecords: true,
            height: '100%',
            width: '50%',
            grouping: true,
            groupingView: {
                groupField: ["BudgetHeadName"],
                groupColumnShow: [false],
                //groupText: ['<b>Tracking Number : {0}</b>']
            },
        });
        // Pager Function
        $("#jqSearch").jqGrid('filterGrid', '#jqGrid', { gridModel: true, gridNames: true, formtype: 'horizontal', autosearch: false, enableSearch: true, enableClear: true });
    });
    function deleteformateadorLink(cellvalue, options, rowObject) {
        var url = '@Html.ActionLink(" ", "Delete", new { id = "_id_" }, new { @class = "deleteItem" })';
        url = url.replace(/_id_/, rowObject[0]);
        return url;
    }
    function viewformatEdiorLink(cellvalue, options, rowObject) {

        var url = '@Html.ActionLink("_name_", "Edit", new { id = "_id_" })';
        url = url.replace(/_name_/, cellvalue);
        url = url.replace(/_id_/, rowObject[0]);
        return url;
    }
</script>
<script type="text/javascript">


    $(function () {

        $("a.deleteItem").on("click", function () {
            deleteLinkObj = $(this);  //for future use
            $('#delete-dialog').dialog('open');
            setTimeout(TriggerSearch, 5000);
            return false; // prevents the default behaviour
        });
    });

    function TriggerSearch() {
        $('#sButton').trigger('click');
    }

    //$(document).ready(function () {
    //    setTimeout(SetText, 7000)
    //})

</script>
<script type="text/javascript">

    function handleDeleteSuccess() {
    }
    function handleDeleteFailure() {
        $("#message").show().html('There is an Ajax call back error!');
    }
    function JsonDelete_OnComplete(context) {
        var obj = eval("(" + context.responseText + ")");
        if (obj.Success) {
            $("#message").show().html("<b>" + obj.Message + "</b>").css("color", "Green");
        }
        else {
            $("#message").show().html("<b>" + obj.Message + "</b>").css("color", "red");
        }
    }

</script>
