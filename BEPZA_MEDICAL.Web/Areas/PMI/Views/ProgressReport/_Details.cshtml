@model BEPZA_MEDICAL.Web.Areas.PMI.ViewModel.ProgressReport.ProgressReportMasterViewModel
@using BEPZA_MEDICAL.Web.Helpers
@if (Model != null && Model.TempAttachmentDetail != null)
{
    foreach (var x in Model.TempAttachmentDetail)
    {
        @Html.Partial("_PartialDetail", x);
    }

}