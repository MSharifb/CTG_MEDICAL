
<script type="text/javascript">

    $(document).ready(function () {

        $('#jqGrid').jqGrid({
            url: '@Url.Action("GetList")',
            datatype: 'json',
            mtype: 'POST',
            colNames: ['Id', 'Financial Year', 'Financial Year', 'Status', 'Delete'],
            colModel: [
                { name: 'Id', index: 'Id', align: 'left', search: false, hidden: true, edittype: 'text' },
                { name: 'FinancialYearId', index: 'FinancialYearId', align: 'left', hidden: true, search: true, edittype: 'select', width: '120px', surl: '@Url.Action("GetFinancialYear")'},
                { name: 'FinancialYearName', index: 'FinancialYearName', align: 'left', hidden: false, search: false, edittype: 'text', formatter: viewformatEdiorLink },
                { name: 'APPStatusId', index: 'APPStatusId', align: 'left', hidden: true, search: true, edittype: 'select', width: '120px', surl: '@Url.Action("GetBudgetStatus")' },
                { name: 'Delete', index: 'Delete', align: 'Center', search: false, hidden: IsDeleteAssigned, sortable: false, width: '40px', edittype: 'image', formatter: deleteformateadorLink }
            ],
            autowidth: true,
            caption: "Annual Procurement Plan",
            rowList: [20, 30, 40, 50, 100],
            pager: $('#jqGridPager'),
            rowNum: 20,
            viewrecords: true,
            height: '100%',
            width: '50%',
            //grouping: true,
            //groupingView: {
            //    groupField: ["Id", "AppHeadName"],
            //    groupColumnShow: [false, false],

            //    groupText: ['<b>Tracking Number : {0}</b>']
            //},
            loadComplete: function () {
                $("a.deleteItem").on('click', function (e) {
                    e.preventDefault();
                    deleteLinkObj = $(this);  //for future use
                    $('#delete-dialog').dialog('open');
                    return false; // prevents the default behaviour
                });
                //var i, group, cssClass, headerIdPrefix = this.id + "ghead_0",
                //    groups = $(this).jqGrid("getGridParam", "groupingView").groups, l = groups.length;

                //for (i = 0; i < l; i++) {
                //    group = groups[i];
                //    if (group.dataIndex == "Id") {
                //        cssClass = "myHeaderStyle";
                //        if (cssClass !== "") {
                //            $("#" + headerIdPrefix + "_" + i).addClass(cssClass);
                //        }
                //    }
                //}
            }
        });
        // Pager Function
        $("#jqSearch").jqGrid('filterGrid', '#jqGrid', { gridModel: true, gridNames: true, formtype: 'horizontal', autosearch: false, enableSearch: true, enableClear: true });

    });


    function deleteformateadorLink(cellvalue, options, rowObject) {
        var url = '@Html.ActionLink(" ", "Delete", new { id = "_APPDetailId_" }, new { @class = "deleteItem" })';
        url = url.replace(/_APPDetailId_/, rowObject[0]);
        return url;
    }

    function viewformatEdiorLink(cellvalue, options, rowObject) {

        var url = '@Html.ActionLink("_name_", "Edit", new { id = "_id_" })';
        url = url.replace(/_name_/, cellvalue);
        url = url.replace(/_id_/, rowObject[0]);
        return url;
    }

</script>

<script type="text/javascript">


    function handleDeleteSuccess() {
    }
    function handleDeleteFailure() {
        $("#message").show().html('There is an Ajax call back error!');
    }
    function JsonDelete_OnComplete(context) {
        var obj = eval("(" + context.responseText + ")");
        if (obj.Success) {
            $("#message").show().html("<b>" + obj.Message + "</b>").css("color", "Green");
            $('#sButton').trigger('click');
        }
        else {
            $("#message").show().html("<b>" + obj.Message + "</b>").css("color", "red");
        }
    }

</script>

<style type="text/css">
    .ui-jqgrid .ui-jqgrid-htable th div {
        word-wrap: break-word; /* IE 5.5+ and CSS3 */
        white-space: pre-wrap; /* CSS3 */
        white-space: -pre-wrap; /* Opera 4-6 */
        white-space: -o-pre-wrap; /* Opera 7 */
        white-space: normal !important;
        height: auto;
        vertical-align: text-top;
        padding-top: 2px;
        padding-bottom: 3px;
    }
</style>

<style type="text/css">
    .myHeaderStyle {
        font-weight: bolder;
        color: black;
        text-decoration: underline;
        cursor: pointer;
        text-transform: uppercase;
    }

        .myHeaderStyle:hover {
            background: #E9E9E9;
        }
</style>