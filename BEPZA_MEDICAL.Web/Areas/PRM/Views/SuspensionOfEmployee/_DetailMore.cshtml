@model BEPZA_MEDICAL.Web.Areas.PRM.ViewModel.SuspensionOfEmployeeViewModel
@using BEPZA_MEDICAL.Web.Helpers
@if (Model != null && Model.SuspensionOfEmployeeDetailList != null)
{
    foreach (var x in Model.SuspensionOfEmployeeDetailList.Where(x => x.HeadType == "Deduction"))
    {
        @Html.Partial("_DeductionDetail", x);
    }

}
