@model BEPZA_MEDICAL.Web.Areas.PRM.ViewModel.BankNameViewModel

@using BEPZA_MEDICAL.Web.Helpers
@if (Model != null && Model.BankBranches != null)
{
    foreach (var x in Model.BankBranches)
    {
        @Html.Partial("_Details", x);
    }
}

