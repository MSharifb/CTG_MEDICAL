<script type="text/javascript">
    $(document).ready(function () {
        $(".datePicker").datepicker({ buttonImage: '@Url.Content("~/Content/Images/calendar-blue.gif")', dateFormat: 'dd/mm/yy', buttonImageOnly: true, showOn: "button", buttonText: 'Choose' });
    });

    $(function () {

        $("a.deleteItem").live("click", function () {
            deleteLinkObj = $(this);  //for future use
            $('#delete-dialog').dialog('open');
            return false; // prevents the default behaviour
        });
        $('#delete-dialog').dialog({
            autoOpen: false, width: 320, resizable: false, modal: true, //Dialog options
            overlay: {
                backgroundColor: '#000',
                opacity: 0.5
            },
            buttons: {
                "Yes": function () {

                    $.post(deleteLinkObj[0].href, function (obj) {  //Post to action

                        if (obj.Success) {
                            $("#message").html("<b>" + obj.Message + "</b>").css("color", "Green");
                            $('#sButton').trigger('click');
                        }
                        else {
                            $("#message").html("<b>" + obj.Message + "</b>").css("color", "red");
                        }
                    });
                    $(this).dialog("close");
                },
                "No": function () {
                    $(this).dialog("close");
                }
            }
        });
    });

</script>
<script type="text/javascript">

    function handleDeleteSuccess() {
    }
    function handleDeleteFailure() {
        $("#message").html('There is an Ajax call back error!');
    }
    function JsonDelete_OnComplete(context) {
        var obj = eval("(" + context.responseText + ")");
        if (obj.Success) {
            $("#message").html("<b>" + obj.Message + "</b>").css("color", "Green");
            $('#sButton').trigger('click');
        }
        else {
            $("#message").html("<b>" + obj.Message + "</b>").css("color", "red");
        }
    }

</script>
<script type="text/javascript">

    //{ name: 'EmployeeInitial', index: 'EmployeeInitial', align: 'left', search: true,  edittype: 'text'},
    //, 'Employee Initial'

    $(document).ready(function () {
        $('#jqGrid').jqGrid({
            url: '@Url.Action("GetList")',
            datatype: 'json',
            mtype: 'POST',
            colNames: ['ID', 'Employee ID', 'Employee Name', 'From Zone', 'To Zone', 'From Designation', 'To Designation', 'Effective Date', 'Editable', 'Delete'],
            colModel: [
                            { name: 'ID', index: 'ID', align: 'left', search: false, hidden: true, edittype: 'text' },
                            { name: 'EmpId', index: 'EmpId', align: 'center', width: '120px', search: true, edittype: 'text', formatter: viewformatEdiorLink },
                            { name: 'EmployeeName', index: 'EmployeeName', align: 'left', search: true, width: '280px', edittype: 'text' },
                            { name: 'FromZoneInfoId', index: 'FromZoneInfoId', width: '240px', align: 'left', search: true, edittype: 'select', surl: '@Url.Action("ZoneInfoView")' },
                            { name: 'ToZoneInfoId', index: 'ToZoneInfoId', width: '240px', align: 'left', search: true, edittype: 'select', surl: '@Url.Action("ZoneInfoView")' },
                            { name: 'FromDesignationId', index: 'FromDesignationId', align: 'left', width: '240px', search: true, edittype: 'select', surl: '@Url.Action("DesignationforView")' },
                            { name: 'ToDesignationId', index: 'ToDesignationId', align: 'left', width: '240px', search: true, edittype: 'select', surl: '@Url.Action("DesignationforView")' },

                            { name: 'EffectiveDate', index: 'EffectiveDate', align: 'center', search: false, edittype: 'text' },
                            { name: 'isEditable', index: 'isEditable', search: false, hidden: true, edittype: 'text' },
                            { name: 'Delete', index: 'Delete', align: 'right', search: false, hidden: IsDeleteAssigned, sortable: false, width: '90px', edittype: 'image', formatter: deleteformateadorLink }

            ],
            autowidth: true,
            caption: "Transfer List",
            rowList: [20, 30, 40, 50, 100],
            pager: $('#jqGridPager'),
            rowNum: 20,
            sortname: 'ID',
            sortorder: 'asc',
            viewrecords: true,
            height: '100%',
            width: '50%',
        });
        // Pager Function
        $("#jqSearch").jqGrid('filterGrid', '#jqGrid', { gridModel: true, gridNames: true, formtype: 'horizontal', autosearch: false, enableSearch: true, enableClear: true });

    });

    datePick = function (elem) {
        $(elem).datepicker({ buttonImage: '@Url.Content("~/Content/Images/calendar-blue.gif")', dateFormat: 'dd/mm/yy', buttonImageOnly: true, showOn: "button", buttonText: 'Choose' });
    }

    function deleteformateadorLink(cellvalue, options, rowObject) {
        if (rowObject[08] == '1') {
            var url = '@Html.ActionLink(" ", "Delete", new { id = "_id_" }, new { @class = "deleteItem" })';
            url = url.replace(/_id_/, rowObject[0]);
            return url;
        }
        return "";
    }

    function viewformatEdiorLink(cellvalue, options, rowObject) {
        //if (rowObject[08] == '1') {

            var url = '@Html.ActionLink("_name_", "Edit", new { id = "_id_" })';
            url = url.replace(/_name_/, cellvalue);
            url = url.replace(/_id_/, rowObject[0]);
            return url;
        //}
        //return rowObject[1];
    }
</script>

<style type="text/css" media="screen">
    th.ui-th-column div {
        white-space: normal !important;
        height: auto !important;
        padding: 2px;
    }
</style>
