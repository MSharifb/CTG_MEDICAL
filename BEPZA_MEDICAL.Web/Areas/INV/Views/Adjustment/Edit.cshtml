@model BEPZA_MEDICAL.Web.Areas.INV.ViewModel.Adjustment.AdjustmentViewModel
@{
    ViewBag.Title = "Edit";
    Layout = "~/Areas/INV/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm("Edit", "Adjustment", FormMethod.Post, new { id = "frm", @encType = "multipart/form-data" }))
{
    <fieldset>
        <div class="GroupBox" id="message">
            @Html.ValidationSummary(false, BEPZA_MEDICAL.Web.Utility.Common.ValidationSummaryHead)
            @if (!string.IsNullOrEmpty(Model.ErrMsg))
            {
                <div id="ErrMsg" class="@Model.errClass">
                    @Model.ErrMsg
                </div>
            }
        </div>
        @Html.Partial("_CreateOrEdit", Model)
        <div class="button-crude">
            @if ((new BEPZA_MEDICAL.Web.Utility.AppConstant()).IsAddAssigned)
            {
                <a href="#" id="btnVoucherPosting" class="btn btn-success"> <i class="fa fa-file-powerpoint-o"></i> Prepare Voucher</a>
                <input type="submit" value="Update" id="btnSave" name="btnSubmit" style="margin-right: 8px;" />
            }
            @*<input type="button" value="Clear" id="btnClear" name="btnClear" />*@
        </div>
    </fieldset>
}
<div class="button-crude button-left">
    @Html.ActionLink("Back to List", "Index")
</div>


