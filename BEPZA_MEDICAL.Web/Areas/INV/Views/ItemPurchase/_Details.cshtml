@model BEPZA_MEDICAL.Web.Areas.INV.ViewModel.ItemPurchaseViewModel
@using BEPZA_MEDICAL.Web.Helpers
@if (Model != null && Model.PurchaseItemDetail != null)
{
    foreach (var x in Model.PurchaseItemDetail)
    {
        @Html.Partial("_PartialDetail", x);
    }

}