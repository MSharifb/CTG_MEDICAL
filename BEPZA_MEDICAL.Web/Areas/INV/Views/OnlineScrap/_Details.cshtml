@model BEPZA_MEDICAL.Web.Areas.INV.ViewModel.ScrapViewModel
@using BEPZA_MEDICAL.Web.Helpers
@if (Model != null && Model.ScrapDetail != null)
{
    foreach (var x in Model.ScrapDetail)
    {
        @Html.Partial("_PartialDetail", x);
    }

}